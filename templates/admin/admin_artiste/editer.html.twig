{% extends 'admin/base_admin.html.twig' %}

{% block contenu %}
<div class="form">
    <h1 class="mb-4">{{ app.request.get('_route') == "admin_artiste_ajouter" ? 'Ajouter':'Modifier'}} les artistes</h1>
    {{ form_start(form) }}
        <div class="col-4">
            {{ form_row(form.nom) }}
        </div>
        <div class="col-4">
            {{ form_row(form.styleMusical) }}
        </div>
        <div class="col-4">
            {{ form_row(form.festivals) }}
        </div>
        <div class="col-4">
            {{ form_row(form.valider) }}
        </div>
    {{ form_end(form) }}
</div>
{% endblock %}